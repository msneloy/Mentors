// Generated by CoffeeScript 2.7.0
(function() {
  /*
    IELTS Band Calculator Logic
  */
  var calculateIeltsBand, roundIeltsBand;

  roundIeltsBand = function(value) {
    return Math.round(value * 2) / 2;
  };

  calculateIeltsBand = function() {
    var average, band, listening, message, reading, speaking, writing;
    listening = parseFloat(document.getElementById("band-listening").value) || 0;
    reading = parseFloat(document.getElementById("band-reading").value) || 0;
    writing = parseFloat(document.getElementById("band-writing").value) || 0;
    speaking = parseFloat(document.getElementById("band-speaking").value) || 0;
    average = (listening + reading + writing + speaking) / 4;
    band = roundIeltsBand(average);
    message = band >= 8 ? "Excellent result — you are ready for most top universities." : band >= 6.5 ? "Strong performance — focus on fine-tuning your weaker modules." : "Keep practicing. Focus on the module(s) where your score is lowest.";
    document.getElementById("band-result-value").textContent = band.toFixed(1);
    return document.getElementById("band-result-message").textContent = message;
  };

  document.addEventListener("DOMContentLoaded", function() {
    var button, inputs;
    inputs = document.querySelectorAll(".band-input");
    button = document.getElementById("calculate-band");
    if (button) {
      button.addEventListener("click", calculateIeltsBand);
    }
    inputs.forEach(function(input) {
      return input.addEventListener("input", calculateIeltsBand);
    });
    return calculateIeltsBand();
  });

}).call(this);
